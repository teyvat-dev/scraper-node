import type { RegionCreateInput } from '../../types/schema_0.4.0/schema';

// The output JSON
export interface RegionOutput extends Omit<RegionCreateInput, 'id'> {}

// Temporary Scraper Data Interfaces
// TODO: Add new types here
